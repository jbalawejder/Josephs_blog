<% if @article.errors.any? %>
<ul>
  <% @article.errors.full_messages.each do |error| %>
    <li><%= error %></li>
  <% end %>
</ul>
<% end %>

<!-- <%= form_for @article, :html => {:class => "form-horizontal center"} do |f| %>
<div class="form-group">
    <%= f.label :title, class: "col-md-4 control-label" %>
    <div class="col-md-8">
        <%= f.text_field :title %>
    </div>
</div>
<div class="form-group">
    <%= f.label :body, class: "col-md-4 control-label" %>
    <div class="col-md-8">
        <%= f.text_area :body %>
    </div>
</div> -->
<%= form_for @article do |f| %>
  <%= f.label :title %>
  <br>
  <%= f.text_field :title %>
  <br>
  <%= f.label :body %>
  <br>
  <%= f.text_area :body, size: "60x12" %>
  <br>
  <%= f.collection_check_boxes :category_ids, Category.all, :id, :name, label: "Choose Category" %>
  <br>
  <%= f.submit "Create Article", class: 'btn btn-primary btn-xs' %>
  <% end %>

  <%end%>
