<h1>Current Articles
<%= link_to 'Create a New Article', new_article_path, class: 'btn btn-primary btn-sm'  %>
<%= link_to 'Create a New Category', new_category_path, class: 'btn btn-primary btn-sm'  %>

</h1>

<% @articles.each do |article|%>

<h2><%= article.title%> </h2>
<p> <%=article.body.truncate_words(30)%> </p>

<%= link_to 'View Full Article', article_path(article), class: 'btn btn-primary btn-xs' %>

<%end%>
